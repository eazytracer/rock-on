---
feature: mobile-redesign-port — Part 2 (flagged forks, now being built in order)
created: 2026-07-06T02:20
status: IN PROGRESS. Part 1 committed as feaa9e4. Building Part 2 top-down.
caveat update: user said "get them all done." Schema-dependent forks (#3 Source filter
  `hidden`, #5 casting hands/access cols, #6 practice enrichment) will need a migration —
  confirm go-ahead when reached (standing "no schema" caveat may be lifted).
---

# Fork #1 — Multi-band context switching (IN PROGRESS)

Model: global `{ personal | band:<id> }`, persisted per user, scopes Songs/Setlists/Shows/
Practices; personal = read-only aggregation across the user's bands (band-tagged). NO schema
(Shows/Practices personal = union, not owned). Default: users WITH bands still land in a band
(preserves login e2e); Personal is a first-class persisted choice; zero-band users = Personal.

Sequence (from research map a3d68e13):

- [x] **Step 1 — AuthContext intent.** `rockon.context` = 'personal'|'<bandId>'; `setContext()`;
      `contextType`; load honors intentional Personal (no auto-band clobber); stale band → Personal.
      Green: type-check 0, 184 ctx/hooks unit tests.
- [x] **Step 2 — ContextSwitcher UI** in Sidebar + MobileHeader brand chip (reload-on-switch interim). + userBands dedup + sidebar layout fix. SongsPage reacts (personal → My Songs, band tab hidden).
      Committed 3e01fe4. Verified: type-check, 810 unit, lint, e2e (52 pass / 1 pre-existing),
      Playwright bidirectional switch.
- [ ] **Step 3 — migrate pages off `localStorage.currentBandId` → `useAuth()`** (removes reload).
- [~] **Step 4 — relax `!currentBandId` hard gates.** SetlistsPage DONE (personal-capable: default
  personal tab, gate relaxed, band tab hidden) + band-less-flow.spec updated (upgrade-path now
  demonstrates unlock via /shows). Shows/Practices still band-gated (personal = aggregation, Step 5).
- [ ] **Step 5 — band-tagged union hooks/services** for Personal aggregation (songs via
      `getUserAccessibleSongs`+tag; setlist/show/practice `getAcrossBands`; widen realtime to band-set).
- [ ] **Step 6 — reconcile Songs/Setlists local tabs** with the global context.
- [ ] e2e updates: `band-less-flow.spec`, `songs/personal-songs.spec`, `songs/personal-mirroring.spec`.

Top regression risks: dual source of truth (localStorage vs context) until Step 3; auto-select
bands[0] clobbering Personal (fixed Step 1); SetlistsPage pre-tab gate; realtime keyed on single
bandId; band-less-flow spec assertions.

# Fork #1 — cross-band aggregation: DEFERRED (simplicity)

Shows/Practices stay band-scoped in personal context (switch to a band to view). Full
cross-band tagged aggregation (3 union hooks + realtime-set) deferred as an optional enhancement.

# Fork #2 — Custom tunings (IN PROGRESS)

- [x] **DB security review** (sub-agent): RLS sound, no Critical/High, recursion safe. Adopted the
      one LOW fix — `trg_tunings_lock_ownership` (pins created_by/context/is_builtin/slug on UPDATE)
      amended INTO the existing `20260704191222_tunings.sql` (one file per feature; local-only).
- [x] **Negative pgTAP matrix** (020-tunings.test.sql → plan 31): built-in tamper, cross-user personal
      writes, cross-band read/write, ownership forgery/re-home (pinned), ownerless reject. All pass.
- [x] **App↔DB parity**: `canonicalTuningId` aligned to SQL `builtin_tuning_slug` + 12 unit tests.
      Green: db reset, all pgTAP, lint:migrations, type-check, 822 unit.
- [ ] **C0 anchored `<Dropdown>`** component (unblocks the tuning picker + fork #3 + the 24 native selects).
- [ ] **Tuning model + TuningService + useTunings** (built-ins local + user customs from the table).
- [ ] **Song form tuning picker** (C0 dropdown, grouped Built-in/Your, writes `tuningId`, keeps
      `guitarTuning` fallback) + Song model `tuningId` + RemoteRepository mapping.
- [ ] **Settings › Tunings manager** + create flow (instrument + string-count → N note pickers).
- [ ] e2e: create custom tuning + pick it on a song; negative (can't edit built-ins in UI).

# Remaining forks (queued, top-down)

#2 Custom tunings app layer (DB done) · #3 Provenance/Source filter [SCHEMA] · #4 Song-notes notepad
state · #5 Casting console [SCHEMA] · #6 Desktop two-pane layouts · #7 C0 anchored Dropdown (24
native selects) · #8 Onboarding solo-first + auto-add · #9 Practice enrichment [SCHEMA] ·
#10 Notifications cross-context. (Full detail: 2026-07-06T01:54_per-page-sync-tasklist.md)
